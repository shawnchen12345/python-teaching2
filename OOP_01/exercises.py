# ===================================================================
# OOP 第1课：类与对象入门 —— 课堂练习
# ===================================================================
# 完成以下练习，巩固 class, __init__, self, 实例属性和实例方法
# 每道题下方的注释是提示，答案在 solutions.py 中
# ===================================================================


# -------------------------------------------------------------------
# 练习 1: 宠物猫 (基础)
# 定义一个 Cat 类：
#   属性: name(名字), age(年龄), energy(精力, 默认100)
#   方法:
#     - eat(food): 吃东西，精力+15，打印 "xx吃了xx，精力恢复到xx"
#     - play(hours): 玩耍，精力-20*hours，打印 "xx玩了xx小时，精力剩余xx"
#                    如果精力不足，打印 "xx太累了，需要休息"
#     - status(): 打印 "猫咪 xx, x岁, 精力: xx"
# 创建两只猫并让它们做不同的事
# -------------------------------------------------------------------
print("=== 练习 1: 宠物猫 ===")
# 在这里写代码:




# -------------------------------------------------------------------
# 练习 2: 银行账户 (中等)
# 定义一个 BankAccount 类：
#   属性: owner(户主), balance(余额, 默认0)
#   方法:
#     - deposit(amount): 存钱，金额必须>0
#     - withdraw(amount): 取钱，余额不足时提醒
#     - transfer(other_account, amount): 转账给另一个 BankAccount 对象
#         - 余额足够时：自己减少，对方增加，打印转账信息
#         - 余额不足时：提醒
#     - __str__(): 返回 "账户(xx): 余额xx元"
# 创建两个账户，测试存、取、转账
# -------------------------------------------------------------------
print("\n=== 练习 2: 银行账户 ===")
# 在这里写代码:




# -------------------------------------------------------------------
# 练习 3: 学生成绩管理 (中等)
# 定义一个 Student 类：
#   属性: name(名字), scores(成绩字典, 默认空字典 {})
#   方法:
#     - add_score(subject, score): 添加/更新某科成绩
#     - get_average(): 返回平均分 (保留1位小数)
#     - get_best_subject(): 返回最高分的科目名
#     - __str__(): 返回 "学生 xx: {各科成绩}"
#   注意: 默认参数陷阱！不能写 def __init__(self, name, scores={})
#         应该用 scores=None，然后在 __init__ 里判断
# -------------------------------------------------------------------
print("\n=== 练习 3: 学生成绩管理 ===")
# 在这里写代码:




# -------------------------------------------------------------------
# 练习 4: 骰子游戏 (综合应用)
# 定义一个 Dice 类：
#   属性: sides(面数, 默认6), history(投掷历史, 列表)
#   方法:
#     - roll(): 随机投一次，记录到 history，返回结果
#     - roll_multiple(times): 投多次，返回结果列表
#     - get_stats(): 返回字典 {"总次数": x, "平均值": x, "最大值": x, "最小值": x}
#     - __str__(): 返回 "x面骰子, 已投掷x次"
#
# 定义一个 DiceGame 类：
#   属性: player_name, dice(一个Dice对象), total_score(总分)
#   方法:
#     - play_round(): 投一次骰子，加到总分，打印信息
#     - show_score(): 打印玩家总分
# -------------------------------------------------------------------
print("\n=== 练习 4: 骰子游戏 ===")
# 在这里写代码:




# -------------------------------------------------------------------
# [挑战题] 练习 5: 简易通讯录
# 定义一个 Contact 类：
#   属性: name, phone, email(可选, 默认"无")
#   方法:
#     - __str__(): 返回格式化的联系人信息
#
# 定义一个 AddressBook 类：
#   属性: contacts(联系人列表)
#   方法:
#     - add_contact(contact): 添加联系人（如果名字重复，提醒更新）
#     - remove_contact(name): 根据名字删除联系人
#     - search(keyword): 在名字和电话中搜索关键词，返回匹配列表
#     - show_all(): 显示所有联系人
#     - __str__(): 返回 "通讯录: xx位联系人"
# -------------------------------------------------------------------
print("\n=== 挑战题: 简易通讯录 ===")
# 在这里写代码:


