# OOP 第4课 教师讲稿：综合实战与设计模式

> **使用说明**: 本讲稿是口语化的教师逐字稿，配合 `lecture_notes.md` 中的代码示例使用。  
> 📌 = 板书/PPT要点 | 💬 = 互动提问 | ⏸️ = 停顿等学生思考 | 💻 = 切换到编辑器现场写代码 | ⏰ = 时间提示

---

## 开场：全面回顾 (15 分钟)

⏰ *00:00 - 00:15*

同学们好！今天是我们 OOP 系列的**最后一课**。

前三课我们学了很多东西。今天有两个任务：
1. 学几个实用的**设计模式**——前人总结的编程"套路"
2. 做一个**综合大项目**——把所有知识串在一起

但在开始之前，我先花几分钟时间帮大家把前三课的内容**串成一条线**。

### OOP 四大特性回顾

💬 **我来提问，你们来回答。OOP 有四大特性，第一个是——？**

⏸️ *逐个提问*

📌 *(白板画思维导图)*

```
OOP 四大特性
│
├── 1. 封装 (Encapsulation)
│     隐藏内部细节，通过接口操作
│     → _, __, @property
│
├── 2. 继承 (Inheritance)
│     子类复用父类功能
│     → class Dog(Animal), super()
│
├── 3. 多态 (Polymorphism)
│     同一方法，不同表现
│     → 方法重写、鸭子类型
│
└── 4. 抽象 (Abstraction)
      定义规范/接口
      → ABC, @abstractmethod
```

好！如果你能把这四个讲清楚，面试的时候 OOP 问题基本没问题了。

### 设计思维五步法

在开始写代码之前，你应该先**想清楚**再动手。我给大家一个口诀：

📌 **写代码前的五问：**
1. 有哪些"东西"？→ 定义**类**
2. 每个东西有什么"特征"？→ 定义**属性**
3. 每个东西能做什么"动作"？→ 定义**方法**
4. 这些东西之间什么**关系**？→ 继承 or 组合
5. 有什么共同行为可以**统一**？→ 多态 + 抽象类

把这个五步法记住，以后做任何项目都先问自己这五个问题。

---

## 第一部分：设计模式入门 (30 分钟)

### 什么是设计模式？ (3 分钟)

⏰ *00:15 - 00:18*

💬 **你们打游戏的时候，有没有发现高手都有固定的"套路"？不管打什么局，起手都是差不多的战术？**

⏸️

编程也一样！**设计模式 (Design Pattern)** 就是程序员前辈们总结出来的"套路"。遇到某种特定问题时，用这个套路来写代码，效果最好。

设计模式这个概念最早来自一本叫《Design Patterns》的经典书，里面总结了 23 种模式。但别紧张，我们今天只学**三种**最常用的。

对于入门来说，这三种就够了。

### 工厂模式 (10 分钟)

⏰ *00:18 - 00:28*

**问题**: 你需要根据不同条件创建不同类型的对象。

**场景**: 比如一个游戏，用户选择角色类型（战士、法师、射手），你要根据选择创建不同的对象。

最笨的方法：

```python
if role == "warrior":
    player = Warrior(name)
elif role == "mage":
    player = Mage(name)
elif role == "archer":
    player = Archer(name)
```

这有什么问题？每次加一个新角色，你就要加一个 `elif`。如果有 50 种角色呢？代码会变成一堵墙。

📌 **工厂模式 = 用一个"工厂"类来统一创建对象**

💻 *写代码*

```python
class Animal:
    def __init__(self, name):
        self.name = name
    def speak(self):
        pass

class Dog(Animal):
    def speak(self):
        return f"{self.name}: 汪汪！"

class Cat(Animal):
    def speak(self):
        return f"{self.name}: 喵喵！"

class Bird(Animal):
    def speak(self):
        return f"{self.name}: 啾啾！"
```

到这里都是之前学过的。现在加一个**工厂**：

```python
class AnimalFactory:
    _registry = {
        "dog": Dog,
        "cat": Cat,
        "bird": Bird,
    }
    
    @classmethod
    def create(cls, animal_type, name):
        animal_class = cls._registry.get(animal_type.lower())
        if animal_class is None:
            raise ValueError(f"未知类型: {animal_type}")
        return animal_class(name)
    
    @classmethod
    def register(cls, animal_type, animal_class):
        """注册新动物类型"""
        cls._registry[animal_type] = animal_class
```

用法：

```python
dog = AnimalFactory.create("dog", "旺财")
cat = AnimalFactory.create("cat", "咪咪")
print(dog.speak())
print(cat.speak())
```

💻 *运行*

💬 **这比一堆 if-elif 好在哪？**

⏸️

两个好处！

第一，我要新加一种动物的时候，**不需要改工厂的代码**！只要：

```python
class Snake(Animal):
    def speak(self):
        return f"{self.name}: 嘶嘶~"

AnimalFactory.register("snake", Snake)  # 注册新类型
snake = AnimalFactory.create("snake", "小青")
```

看到没？工厂的 `create` 方法**一行都没改**。这就是"**对扩展开放，对修改关闭**"——上节课说的开闭原则。

第二，创建对象的逻辑**集中管理**了。以后要改创建逻辑（比如加日志、加缓存），只改工厂一个地方就好。

### 观察者模式 (10 分钟)

⏰ *00:28 - 00:38*

**问题**: 一个对象的状态变了，需要通知其他多个对象。

**场景**: 游戏里角色掉血了——UI 要更新血条，音效系统要播放受伤音效，成就系统要检查是否触发"濒死"成就。这么多系统怎么通知？

最笨的方法：在 `take_damage` 方法里写：

```python
def take_damage(self, damage):
    self.hp -= damage
    update_ui(self)           # 通知 UI
    play_sound("hurt")        # 通知音效
    check_achievement(self)   # 通知成就
    log_event(self)           # 通知日志
```

一有新系统加进来，你就得改 `take_damage`。30 个系统就写 30 行通知代码。

📌 **观察者模式 = 发布-订阅机制**

角色只管"喊一声"——"我受伤了！"  
谁关心谁来听，角色不需要知道有多少个听众。

💻 *写代码*

```python
class EventSystem:
    def __init__(self):
        self._listeners = {}
    
    def on(self, event, callback):
        """注册监听"""
        if event not in self._listeners:
            self._listeners[event] = []
        self._listeners[event].append(callback)
    
    def emit(self, event, data=None):
        """触发事件"""
        if event in self._listeners:
            for callback in self._listeners[event]:
                callback(data)
```

这个 EventSystem 就是一个"广播站"。你可以**注册**监听（相当于"订阅频道"），也可以**触发**事件（相当于"广播消息"）。

```python
class Player:
    def __init__(self, name):
        self.name = name
        self.hp = 100
        self.events = EventSystem()
    
    def take_damage(self, damage):
        self.hp -= damage
        # 只需要"广播"一条消息，不需要知道谁在听
        self.events.emit("damage", {"name": self.name, "hp": self.hp})
```

然后各个系统自己来"订阅"：

```python
def ui_update(data):
    print(f"[UI] {data['name']} HP: {data['hp']}")

def play_sfx(data):
    print(f"[SFX] 播放受伤音效！")

p = Player("勇者")
p.events.on("damage", ui_update)  # UI订阅伤害事件
p.events.on("damage", play_sfx)   # 音效订阅伤害事件

p.take_damage(30)
```

💻 *运行*

Player 的 `take_damage` 里面**完全没有**写 UI 或音效的代码！它只管"广播"。谁想听谁自己来注册。

新加一个日志系统？直接注册就行，`take_damage` **一行都不用改**。

```python
def log_event(data):
    print(f"[LOG] {data}")

p.events.on("damage", log_event)  # 新加的监听
```

这就是观察者模式的精髓。

### 策略模式 (7 分钟)

⏰ *00:38 - 00:45*

最后一个，**策略模式**。

**问题**: 同一个操作有多种做法，需要灵活切换。

**场景**: 排序——你可以用冒泡排序、快速排序、归并排序。不同场景用不同算法最优。

📌 **策略模式 = 把算法封装成类，可以随时替换**

💻 *快速展示*

```python
from abc import ABC, abstractmethod

class SortStrategy(ABC):
    @abstractmethod
    def sort(self, data):
        pass

class BubbleSort(SortStrategy):
    def sort(self, data):
        # 冒泡排序实现...
        print("使用冒泡排序")
        return sorted(data)

class QuickSort(SortStrategy):
    def sort(self, data):
        print("使用快速排序")
        return sorted(data)

class Sorter:
    def __init__(self, strategy):
        self.strategy = strategy
    
    def set_strategy(self, strategy):
        self.strategy = strategy   # 随时切换！
    
    def sort(self, data):
        return self.strategy.sort(data)
```

```python
sorter = Sorter(BubbleSort())
sorter.sort([3, 1, 4])           # 使用冒泡排序

sorter.set_strategy(QuickSort())  # 切换策略
sorter.sort([3, 1, 4])           # 使用快速排序
```

算法可以像**零件一样更换**，Sorter 不需要知道具体用什么算法——传什么策略就用什么策略。

📌 **三个设计模式总结：**
| 模式 | 一句话 | 解决什么问题 |
|------|--------|-------------|
| 工厂 | 用工厂统一创建对象 | 避免一堆 if-elif 创建不同类型 |
| 观察者 | 发布-订阅，广播消息 | 一个变化需要通知多方 |
| 策略 | 算法可替换 | 同一操作有多种做法 |

---

## 第二部分：综合实战——在线商城系统 (45 分钟)

### 需求分析 (10 分钟)

⏰ *00:45 - 00:55*

好，设计模式学完了。接下来是今天的重头戏——我们来**一起设计**一个项目。

项目是：**迷你在线商城**。

💬 **一个在线商城，比如京东或者淘宝，都有哪些"东西"？大家来说说。**

⏸️ *让学生说，在白板上记录*

……好，我来整理一下：

📌 *(白板整理)*

**五问分析法：**

**1. 有哪些"东西"（类）？**
- 商品 (Product)
- 用户 (User)
- 购物车 (ShoppingCart)
- 订单 (Order)

**2. 每个东西有什么"特征"（属性）？**
- 商品：名称、价格、库存
- 用户：用户名、余额
- 购物车：里面的商品列表
- 订单：订单号、状态、总价

**3. 每个东西能做什么"动作"（方法）？**
- 商品：计算折后价
- 用户：充值、扣款
- 购物车：添加、删除、算总价
- 订单：支付、发货、收货

**4. 它们之间什么关系？**
- Book、Electronics 都**是** Product → **继承**
- RegularUser、VIPUser 都**是** User → **继承**
- 购物车**有**商品列表 → **组合**
- 订单**有**用户和商品 → **组合**

**5. 有什么共同行为可以统一？**
- 所有 Product 都有 `get_discount()` → **抽象方法**
- 所有 User 都有 `get_discount_rate()` → **多态**

看到了吗？前三课学的所有东西全都用上了！

📌 *(画类图)*
```
Product (ABC)
├── Book        → discount 10%
├── Electronics → discount 5%
└── Clothing    → discount 20%

User
├── RegularUser → 无额外折扣
└── VIPUser     → VIP等级折扣 + 积分

ShoppingCart → 持有 Product 列表
Order → 持有 User + 商品 + 状态
```

### 一起写代码 (35 分钟)

⏰ *00:55 - 01:30*

好，现在我们来一起写代码。我在前面写，你们跟着打。

💻 *新建文件 `mall.py`*

#### 第一步：商品体系

```python
from abc import ABC, abstractmethod

class Product(ABC):
    def __init__(self, name, price, stock=10):
        self.name = name
        self.price = price
        self.stock = stock
    
    @abstractmethod
    def get_discount(self):
        pass
    
    @property
    def final_price(self):
        return round(self.price * (1 - self.get_discount()), 2)
    
    def __str__(self):
        return f"{self.name} ¥{self.price} (折后¥{self.final_price})"
```

💬 **这里用到了什么？**

⏸️

- 抽象类（ABC + @abstractmethod）—— 第3课
- property —— 第2课
- `__str__` —— 第1课

全都串起来了！

```python
class Book(Product):
    def __init__(self, name, price, author):
        super().__init__(name, price)
        self.author = author
    
    def get_discount(self):
        return 0.1  # 图书9折

class Electronics(Product):
    def __init__(self, name, price, brand):
        super().__init__(name, price)
        self.brand = brand
    
    def get_discount(self):
        return 0.05  # 电子产品95折
```

这里用到了继承和方法重写——第3课的内容。

#### 第二步：用户体系

```python
class User:
    def __init__(self, username, balance=0):
        self.username = username
        self._balance = balance
    
    @property
    def balance(self):
        return self._balance
    
    def add_funds(self, amount):
        self._balance += amount
    
    def get_discount_rate(self):
        return 1.0  # 无折扣

class VIPUser(User):
    def __init__(self, username, balance=0, vip_level=1):
        super().__init__(username, balance)
        self.vip_level = vip_level
    
    def get_discount_rate(self):
        discounts = {1: 0.95, 2: 0.90, 3: 0.85}
        return discounts.get(self.vip_level, 1.0)
```

这里用到了封装（`_balance`）、property、继承、多态（`get_discount_rate`）。

#### 第三步：购物车

```python
class ShoppingCart:
    def __init__(self):
        self._items = {}  # {Product: quantity}
    
    def add(self, product, quantity=1):
        if product.stock < quantity:
            print(f"❌ {product.name} 库存不足！")
            return
        if product in self._items:
            self._items[product] += quantity
        else:
            self._items[product] = quantity
        print(f"✅ 添加 {product.name} × {quantity}")
    
    def get_total(self, user=None):
        total = sum(p.final_price * q for p, q in self._items.items())
        if user:
            total *= user.get_discount_rate()  # 多态！
        return round(total, 2)
    
    def __len__(self):
        return len(self._items)
    
    def __str__(self):
        lines = ["🛒 购物车:"]
        for p, q in self._items.items():
            lines.append(f"  {p.name} × {q} = ¥{p.final_price * q:.2f}")
        lines.append(f"  小计: ¥{self.get_total():.2f}")
        return "\n".join(lines)
```

这里用到了组合、魔术方法（`__len__`、`__str__`）、多态。

#### 测试

```python
book = Book("Python编程", 89.9, "Eric")
laptop = Electronics("MacBook", 14999, "Apple")

user = VIPUser("张总", vip_level=2)
user.add_funds(20000)

cart = ShoppingCart()
cart.add(book, 2)
cart.add(laptop, 1)

print(cart)
print(f"VIP2折后: ¥{cart.get_total(user)}")
```

💻 *运行*

你看，`cart.get_total(user)` 传了一个用户进去。如果是普通用户，`get_discount_rate()` 返回 1.0。如果是 VIP2，返回 0.9。**同一个方法调用，不同结果**——多态！

而且 `final_price` 里面用了 `get_discount()`，不同商品有不同折扣——又是多态！

整个系统到处都是 OOP 的特性在协同工作。

---

## 第三部分：课程总结与展望 (15 分钟)

### SOLID 原则 (5 分钟)

⏰ *01:30 - 01:35*

最后说几个设计原则。专业程序员遵循的五条准则，缩写为 **SOLID**：

📌 *(快速讲，不展开太多)*

| 字母 | 原则 | 通俗说法 |
|------|------|---------|
| S | 单一职责 | 一个类只做一件事，别搞"万能类" |
| O | 开闭原则 | 加功能写新代码，别改旧代码 |
| L | 里氏替换 | 子类能替代父类使用，不能搞破坏 |
| I | 接口隔离 | 别让类实现它不需要的方法 |
| D | 依赖倒置 | 依赖抽象类，不直接依赖具体类 |

现在不需要全记住。等你写大项目的时候，遇到 bug 查一查，你就明白这些原则的价值了。

### 学完 OOP 之后学什么？ (5 分钟)

⏰ *01:35 - 01:40*

给大家画一个学习路线：

📌 *(板书)*
```
你现在在这里
     ↓
[OOP 基础] ✅ 已完成！
     ↓
[异常处理] 自定义异常类（继承 Exception）
     ↓
[文件操作 + JSON] 数据持久化
     ↓
[选一条路深入]
  ├── 游戏方向 → Pygame（都是类和对象）
  ├── Web方向 → Flask / Django（MVC 模式）
  ├── 数据方向 → Pandas, Matplotlib
  └── AI方向 → PyTorch（全是 OOP）
```

不管你走哪条路，OOP 都是基础中的基础。你今天学的东西，以后会**天天用**。

### 给同学们的建议 (5 分钟)

⏰ *01:40 - 01:45*

我给大家几条发自内心的建议：

**第一，不要为了 OOP 而 OOP。**

一个 20 行的小脚本，你硬要搞出 5 个类——这不是高级，这是**过度设计**。面向过程能解决的简单问题，就用面向过程。用 OOP 是因为项目**需要**，不是为了装。

**第二，多读好代码。**

去 GitHub 上找 Python 的优秀开源项目，看人家的类怎么设计的。比如 `requests` 库的源码就是 OOP 教科书级的示范。

**第三，多写项目。**

光看讲义是学不会编程的。你必须自己动手写，遇到 bug、想解决方案、改完再测——这个过程才是真正学东西的过程。

---

## 结课：布置大作业 (15 分钟)

⏰ *01:45 - 02:00*

好！四课的内容到这里就全部讲完了。最后布置一个**课后大作业**，二选一：

📌 **选项 A：完整在线商城系统**（练习3）
- 实现完整的商品、用户、购物车、订单系统
- 需要支持支付、发货、收货、退款的完整流程
- 需要用到：抽象类、继承、多态、封装、property

📌 **选项 B：文字冒险游戏**（练习4）
- 设计房间、物品、背包、玩家
- 实现移动、拾取、使用物品
- 至少 4 个房间相互连通
- 需要交互式命令输入
- 需要用到：组合、封装、魔术方法

这两个项目选**你更感兴趣**的那个做。做完之后把代码发给我，我给你做代码评审。

💬 **有什么问题吗？**

*（答疑时间）*

---

## 下课

最后再说一遍：

OOP 的核心不是那些语法——`class`、`self`、`super()` 这些你忘了可以随时查。

真正重要的是**思维方式**——遇到问题先想"有哪些对象，它们有什么属性和行为，它们之间什么关系"。

这种思维方式，不管你以后用 Python、Java、C++ 还是什么语言，都是通用的。

好了同学们，OOP 课程到此结束。谢谢大家，下课！🎉
