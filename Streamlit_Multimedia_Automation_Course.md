# Streamlit 实战日课：多媒体与自动化办公 (4小时)

**核心目标**: 跳出“聊天机器人”的思维定势，利用 Python 丰富的生态库 (PIL, Pandas, os)，把 Streamlit 变成一个真正的**生产力工具**。

**前置要求**:
*   掌握 Streamlit 基础 (会写 `st.title`, `st.button`)。
*   了解 Python 的 `os` 模块和 `Pillow` 库基础 (如果没有，课程中会简讲)。

---

## 📅 课程大纲

### 第一部分：魔法照相馆 - 图像处理 (1.5小时)
**目标**: 制作一个能处理用户上传/拍摄照片的 App。
1.  **上传与拍摄**: 掌握 `st.file_uploader` (传图) 和 `st.camera_input` (拍照)。
2.  **图像处理**: 使用 `Pillow` 库进行：
    *   **黑白化** (Grayscale)
    *   **旋转** (Rotate)
    *   **模糊** (Blur)
3.  **下载结果**: 使用 `st.download_button` 把处理好的图片存回本地。

### 第二部分：办公室救星 - 自动化文件处理 (1.5小时)
**目标**: 解决真实办公痛点——批量重命名和整理文件。
1.  **场景模拟**: "老板给了我 100 个乱七八糟的发票 PDF，要我全改成【日期-金额.pdf】。"
2.  **上传机制**: 允许上传多个文件 (`accept_multiple_files=True`)。
3.  **重命名逻辑**:
    *   简单的字符串替换（如把 "副本" 去掉）。
    *   (进阶) 正则表达式提取。
4.  **打包下载**: 使用 `zipfile` 库把处理好的文件打包成一个 `.zip` 供用户下载。

### 第三部分：炫酷图表 - Plotly 数据可视化 (1小时)
**目标**: 超越 Streamlit 自带的简单图表，制作可交互的商业级看板。
1.  **Plotly 基础**: 为什么它比 Matplotlib 好？(因为它是活的，鼠标放上去能看数字)。
2.  **实战**: 股票/销售数据 K 线图。

---

## 📝 讲师授课脚本 (Lecture Notes)

### 📸 第一部分：魔法照相馆 (Image Processing)

**开场**:
"大家平时修图可能用美图秀秀，但如果要给 1000 张图加水印呢？或者是做一个属于你自己的滤镜 App？今天我们用 Python 几行代码搞定它。"

**核心代码演示 (app_image.py)**:
```python
import streamlit as st
from PIL import Image, ImageFilter

st.title("🧙‍♂️ 魔法照相馆")

# 1. 上传图片
uploaded_file = st.file_uploader("上传一张图片", type=['jpg', 'png'])

if uploaded_file:
    image = Image.open(uploaded_file)
    st.image(image, caption="原图", width=300)

    # 2. 侧边栏滤镜
    filter_type = st.sidebar.radio("选择滤镜", ["无", "黑白", "模糊", "轮廓"])

    if filter_type == "黑白":
        processed_image = image.convert("L")
    elif filter_type == "模糊":
        processed_image = image.filter(ImageFilter.BLUR)
    elif filter_type == "轮廓":
        processed_image = image.filter(ImageFilter.CONTOUR)
    else:
        processed_image = image

    # 3.展示结果
    st.image(processed_image, caption="效果图", width=300)
    
    # 4. 重点：如何把 PIL 图片转成字节流供下载
    import io
    buf = io.BytesIO()
    processed_image.save(buf, format="PNG")
    byte_im = buf.getvalue()

    st.download_button(
        label="下载处理后的图片",
        data=byte_im,
        file_name="magic_photo.png",
        mime="image/png"
    )
```
**教学重点**:
*   解释 `Session State` 在这里的作用（如果不存，每次点按钮图片可能消失，虽然这个简单例子没用到，但复杂修图需要）。
*   解释 `io.BytesIO()`：内存里的“虚拟文件”，不用存到硬盘就能下载，速度极快。

---

### 📂 第二部分：文件批量重命名器 (File Automation)

**痛点引入**:
"谁在工作里受过‘改文件名’的苦？今天我们要写一个神器，以后这种活儿一秒钟搞定。"

**核心代码演示 (app_rename.py)**:
```python
import streamlit as st
import zipfile
import io

st.title("📂 文件名清洗神器")

# 1.多文件上传
files = st.file_uploader("上传一堆文件", accept_multiple_files=True)

# 2. 输入替换规则
replace_str = st.text_input("要把文件名里的哪个词删掉？", value="副本")

if files and st.button("开始清洗"):
    # 创建一个内存里的压缩包
    zip_buffer = io.BytesIO()
    
    with zipfile.ZipFile(zip_buffer, "w") as zf:
        for file in files:
            # 获取原文件名
            old_name = file.name
            # 执行改名逻辑 (这里是最简单的字符串替换)
            new_name = old_name.replace(replace_str, "")
            
            # 写入压缩包 (writestr: 直接把文件内容写进去)
            zf.writestr(new_name, file.getvalue())
            
            st.write(f"✅ {old_name} -> {new_name}")

    # 3. 提供下载
    st.download_button(
        "下载打包好的文件",
        data=zip_buffer.getvalue(),
        file_name="cleaned_files.zip",
        mime="application/zip"
    )
```
**教学重点**:
*   `accept_multiple_files=True`: 让上传框支持多选。
*   `zipfile` + `io.BytesIO`: 又是内存操作！不产生垃圾文件，极度优雅。

---

### 📈 第三部分： Plotly 交互图表 (Interactive Charts)

**对比**:
"Streamlit 自带的 `st.line_chart` 很方便，但如果你想自定义颜色、想鼠标悬停显示具体数字、想缩放查看细节，就得用 Plotly。"

**代码 (app_chart.py)**
```python
import streamlit as st
import plotly.express as px
import pandas as pd

df = pd.DataFrame({
    "水果": ["苹果", "香蕉", "橘子", "苹果", "香蕉"],
    "销量": [10, 20, 15, 12, 22],
    "城市": ["北京", "北京", "北京", "上海", "上海"]
})

# 一行代码画出交互式柱状图
fig = px.bar(df, x="水果", y="销量", color="城市", barmode="group")

st.plotly_chart(fig)
```
**教学重点**:
*   `color="城市"`: 自动分组并上色，非常智能。
*   `barmode="group"`: 并排显示而不是堆叠。

---

## 🎉 总结作业
做一 **"个人工具箱"** 网页：
*   左侧边栏导航：[图片处理, 文件整理, 数据看板]。
*   把今天写的三个功能集成到一个 App 里。
