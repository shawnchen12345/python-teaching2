# Day 3: Python 函数入门 —— 从重复代码到优雅封装

**课程时长**: 8 小时 (上午 4 小时 + 下午 4 小时)
**适用对象**: 编程零基础或初学者
**教学目标**: 理解函数的概念，掌握定义、调用、参数传递、返回值、作用域等核心技能

---

# 🌅 上午课程 (4小时)

---

## 第一章：为什么需要函数？(1小时)
（询问数学中是否学过函数，函数的作用）
### 1.1 没有函数的世界

**场景演示**: 给三位同学打招呼

```python
print("你好，小明！")
print("欢迎来到Python世界！")
print("祝你学习愉快！")

print("你好，小红！")
print("欢迎来到Python世界！")
print("祝你学习愉快！")

print("你好，小刚！")
print("欢迎来到Python世界！")
print("祝你学习愉快！")
```

**问题讨论**:
- 如果有100位同学怎么办？
- 如果要把"祝你学习愉快"改成"加油"，要改几处？

**引出核心问题**: 代码重复 → 难维护 → 易出错

---

### 1.2 函数是什么？

**核心定义**: 函数 = 给一段代码起个名字，以后可以随时"喊"（call）它来执行

**生活类比**:
| 生活场景 | 对应函数概念 |
|---------|-------------|
| 微波炉的"快速加热"按钮 | 一个预设好的函数 |
| 计算器的"开方"键 | 接收输入，返回结果的函数 |
| 手机里的"一键静音" | 无需参数的函数 |

**DRY原则**: Don't Repeat Yourself (不要重复你自己)

---

### 1.3 第一个函数

**语法解析**:

```python
def say_hello():
    print("你好！")
    print("欢迎学习Python！")
```

| 组成部分 | 说明 |
|---------|------|
| `def` | 关键字，告诉Python"我要定义函数" |
| `say_hello` | 函数名（自己起的名字） |
| `()` | 参数列表（暂时为空） |
| `:` | 冒号，不能忘！ |
| 缩进的代码 | 函数体（函数要做的事情） |

---

### 1.4 定义 vs 调用

**关键区别**:
- **定义函数**: 只是"教会"Python这个函数怎么做，不会立刻执行
- **调用函数**: 真正让函数"跑起来"

```python
# 定义（此时不会打印任何东西）
def greet():
    print("Hello!")

# 调用（现在才会打印）
greet()
greet()
greet()
```

**📝 随堂练习 1**: 
1. 定义函数 `print_line()` 打印一行 20 个 `-`
2. 定义函数 `welcome()` 打印三句欢迎语
3. 调用这两个函数

---

## 第二章：参数 —— 让函数更灵活 (1.5小时)

### 2.1 什么是参数？

**问题**: 上面的 `say_hello()` 只能说固定的话，如何让它对不同的人说不同的话？

**解决方案**: 使用参数！

```python
def greet(name):
    print(f"你好，{name}！")

greet("小明")  # 你好，小明！
greet("Alice") # 你好，Alice！
```

**术语解释**:
- **形参 (Parameter)**: 定义函数时括号里的变量 → `name`
- **实参 (Argument)**: 调用函数时传入的具体值 → `"小明"`

---

### 2.2 多个参数

```python
def introduce(name, age):
    print(f"我叫{name}，今年{age}岁")

introduce("小红", 16)
```

**⚠️ 重要规则**: 调用时参数的**顺序**必须和定义时一致！

---

### 2.3 关键字参数

如果记不住顺序怎么办？使用**关键字参数**：

```python
introduce(age=16, name="小红")  # 顺序可以变！
```

**优点**: 代码更清晰，不容易传错

---

### 2.4 默认参数

给参数设置"默认值"，调用时可以不传：

```python
def power(base, exp=2):
    print(f"{base}的{exp}次方 = {base ** exp}")

power(5)      # 5的2次方 = 25 (使用默认值)
power(5, 3)   # 5的3次方 = 125 (覆盖默认值)
```

**⚠️ 规则**: 默认参数必须放在非默认参数的**后面**！

---

### 2.5 类型提示 (选讲)

让代码更易读（Python 3.5+）：

```python
def add(a: int, b: int) -> int:
    return a + b
```

**� 随堂练习 2**:
1. 写函数 `area(width, height)` 计算并打印矩形面积
2. 写函数 `student_info(name, grade="一年级")` 打印学生信息
3. 写函数 `calc(a, b, op)` 根据 op (+, -, *) 打印计算结果

---

## 第三章：返回值 —— 让函数"交作业" (1.5小时)

### 3.1 print vs return

**问题**: 下面两个函数有什么区别？

```python
# 版本A：只打印
def add_v1(a, b):
    print(a + b)

# 版本B：返回结果
def add_v2(a, b):
    return a + b
```

**测试**:
```python
result1 = add_v1(3, 5)  # 打印8，但 result1 是 None
result2 = add_v2(3, 5)  # 不打印，但 result2 是 8
print(result2 * 2)      # 可以继续用！输出 16
```

**金句记忆**:
- 🖨️ `print` 是给**人**看的
- 📦 `return` 是给**程序**用的

---

### 3.2 返回值的用途

返回值可以：
- 赋值给变量
- 参与后续计算
- 作为其他函数的参数

```python
def double(x):
    return x * 2

result = double(5)        # 赋值
print(double(3) + 10)     # 参与计算
print(double(double(2)))  # 作为参数
```

---

### 3.3 多个返回值

Python 允许返回多个值（本质是返回元组）：

```python
def get_user():
    return "Alice", 18

name, age = get_user()  # 解包
print(name)  # Alice
print(age)   # 18
```

---

### 3.4 return 会结束函数

```python
def check(x):
    if x > 0:
        return "正数"
    return "非正数"  # 如果上面没return，才执行到这里
```

**📝 随堂练习 3**:
1. 写函数 `max_of_two(a, b)` **返回**较大的数
2. 写函数 `circle_area(r)` **返回**圆面积 (π ≈ 3.14)
3. 写函数 `swap(a, b)` **返回**交换后的两个值

---

# 🌆 下午课程 (4小时)

---

## 第四章：变量作用域 (1小时)

### 4.1 局部变量 vs 全局变量

```python
x = 100  # 全局变量

def test():
    y = 50  # 局部变量
    print(f"函数内: x={x}, y={y}")

test()
print(x)   # 100
print(y)   # ❌ 报错！y 在函数外不存在
```

**规则**:
- **局部变量**: 在函数内部定义，只能在函数内使用
- **全局变量**: 在函数外部定义，整个文件都能读取

---

### 4.2 同名变量的"遮蔽"

```python
x = 100

def test():
    x = 50  # 这是新建的局部变量，不是修改全局的！
    print(f"函数内: {x}")

test()          # 50
print(f"函数外: {x}")  # 100 (没变)
```

---

### 4.3 global 关键字

如果真的想在函数内**修改**全局变量：

```python
count = 0

def add_one():
    global count  # 声明：我要用全局的 count
    count += 1

add_one()
add_one()
print(count)  # 2
```

**⚠️ 建议**: 新手阶段尽量少用 `global`，容易造成混乱。

---

### 4.4 LEGB规则 (了解)

Python 查找变量的顺序：
1. **L**ocal (局部)
2. **E**nclosing (外层函数，如果有嵌套)
3. **G**lobal (全局)
4. **B**uilt-in (内置，如 `print`, `len`)

**📝 随堂练习 4**:
```python
x = 10
def func():
    x = 5
    print(x)
func()
print(x)
```
问：输出什么？为什么？

---

## 第五章：函数进阶技巧 (1.5小时)

### 5.1 函数调用函数

函数可以像工具一样组合使用：

```python
def square(x):
    return x * x

def sum_of_squares(a, b):
    return square(a) + square(b)

print(sum_of_squares(3, 4))  # 9 + 16 = 25
```

---

### 5.2 文档字符串 (Docstring)

给函数写"说明书"：

```python
def add(a, b):
    """
    计算两个数的和
    
    参数:
        a: 第一个数
        b: 第二个数
    返回:
        两数之和
    """
    return a + b

help(add)  # 可以查看说明
```

---

### 5.3 不定长参数 *args

当你不知道会传入多少个参数时：

```python
def sum_all(*nums):
    total = 0
    for n in nums:
        total += n
    return total

print(sum_all(1, 2))        # 3
print(sum_all(1, 2, 3, 4))  # 10
```

**原理**: `*nums` 把所有位置参数打包成一个**元组**

---

### 5.4 不定长关键字参数 **kwargs

当你想接收任意数量的"名=值"参数时：

```python
def show_info(**kwargs):
    for key, value in kwargs.items():
        print(f"{key}: {value}")

show_info(name="Alice", age=18, city="Beijing")
```

**原理**: `**kwargs` 把所有关键字参数打包成一个**字典**

**📝 随堂练习 5**:
1. 写函数 `my_sum(*nums)` 返回所有数的和
2. 写函数 `build_profile(name, **info)` 打印用户信息

---

## 第六章：递归初探 (1小时)

### 6.1 什么是递归？

**定义**: 函数调用自己

**经典案例: 计算阶乘**
- 5! = 5 × 4 × 3 × 2 × 1
- 5! = 5 × 4!

```python
def factorial(n):
    if n == 1:
        return 1
    return n * factorial(n - 1)

print(factorial(5))  # 120
```

---

### 6.2 递归的两个要素

1. **终止条件**: `if n == 1: return 1`
2. **递归调用**: `factorial(n - 1)`

**⚠️ 警告**: 没有终止条件 = 无限循环 → 程序崩溃！

---

### 6.3 递归 vs 循环

同样计算阶乘：

```python
# 循环版
def factorial_loop(n):
    result = 1
    for i in range(1, n + 1):
        result *= i
    return result
```

**对比**:
- 递归：代码简洁，但可能有性能问题
- 循环：效率高，但代码可能更长

**📝 随堂练习 6**:
用递归实现：计算从 1 加到 n 的和

---

## 第七章：综合实战 (0.5小时)

### 7.1 代码重构案例

**原始代码** (面条式):
```python
name = input("姓名：")
math = int(input("数学成绩："))
english = int(input("英语成绩："))
total = math + english
avg = total / 2
print(f"{name} 总分：{total}")
print(f"平均分：{avg}")
```

**函数化改造**:
```python
def get_scores():
    math = int(input("数学成绩："))
    english = int(input("英语成绩："))
    return math, english

def calc_stats(math, english):
    total = math + english
    avg = total / 2
    return total, avg

def print_report(name, total, avg):
    print(f"{name} 总分：{total}")
    print(f"平均分：{avg}")

# 主程序
name = input("姓名：")
math, english = get_scores()
total, avg = calc_stats(math, english)
print_report(name, total, avg)
```

**好处**: 结构清晰、易于测试、便于修改

---

## 课程总结

| 核心概念 | 要点 |
|---------|------|
| 函数定义 | `def 函数名(参数): 函数体` |
| 参数 | 位置参数、关键字参数、默认参数、*args、**kwargs |
| 返回值 | `return` 把结果交给调用者 |
| 作用域 | 局部变量 vs 全局变量，LEGB规则 |
| 递归 | 函数调用自己，必须有终止条件 |

---

## 课后作业

1. 写函数 `is_prime(n)` 判断是否为素数
2. 写函数 `circle_area(r, pi=3.14)` 计算圆面积
3. 写函数 `seconds_to_time(s)` 将秒数转换为 "时:分:秒" 格式
4. 用函数重写"猜数字游戏"
